# Copy this file to .env and fill in real values. .env is gitignored.

# Your home origin (used for every travel-time calculation).
HOME_ADDRESS="123 Example St, Whittier, CA 90601"
HOME_LAT=
HOME_LNG=

# Maps / geocoding provider key (whichever you choose).
MAPS_API_KEY=

# Optional: LLM key for fuzzy school-name matching / job-text parsing.
LLM_API_KEY=
